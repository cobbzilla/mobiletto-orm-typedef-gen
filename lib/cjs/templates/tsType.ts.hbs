{{#if disclaimer}}// {{ disclaimer }}
{{/if}}export type {{ name }}Type = {{#if root}}{
    _meta?: {
        id: string;
        version: string;
        removed?: boolean;
        ctime: number;
        mtime: number;
    };
{{else}}{
{{/if}}{{#each fields}}    {{this.name}}{{#if this.optional}}?{{/if}}: {{#if this.typeIsObject}}{{{ ../name }}}_{{{this.name}}}Type{{else if this.typeIsArray}}{{this.valueType}}[]{{else}}{{this.type}}{{/if}};
{{/each}}
};
